{
  "name": "aisanta",
  "version": "1.0.0",
  "description": "A new project to use for our internal gaming for secret santa",
  "main": "server/app.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "start": "node server/app.js",
    "dev": "node server/app.js",
    "build": "cd client && npm install && npm run build",
    "install-all": "npm install && cd client && npm install",
    "setup-auth": "node server/hashPasswords.js",
    "postinstall": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "secret-santa",
    "gift-exchange",
    "christmas"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@azure/cosmos": "^4.9.0",
    "@azure/openai": "^2.0.0",
    "@modelcontextprotocol/sdk": "^1.24.3",
    "bcrypt": "^6.0.0",
    "body-parser": "^2.2.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "nodemailer": "^7.0.11",
    "openai": "^6.10.0",
    "socket.io": "^4.8.1"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  }
}
